{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://sdk.elu.dev/contracts/v2/replay.schema.json",
    "title": "ELU SDK replay chunk envelope v2",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "schemaVersion",
        "replayId",
        "sessionId",
        "chunkId",
        "sequence",
        "startedAt",
        "endedAt",
        "identity",
        "contextRevision",
        "codec",
        "compression",
        "contentEncoding",
        "payload",
        "privacy",
        "versions"
    ],
    "properties": {
        "schemaVersion": { "const": 2 },
        "replayId": { "type": "string", "minLength": 1, "maxLength": 256 },
        "sessionId": { "type": "string", "minLength": 1, "maxLength": 256 },
        "chunkId": { "type": "string", "minLength": 1, "maxLength": 256 },
        "sequence": { "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
        "startedAt": { "type": "string", "format": "date-time" },
        "endedAt": { "type": "string", "format": "date-time" },
        "identity": {
            "type": "object",
            "additionalProperties": false,
            "required": ["anonymousId", "userId", "revision"],
            "properties": {
                "anonymousId": { "type": "string", "minLength": 1, "maxLength": 256 },
                "userId": { "type": ["string", "null"], "minLength": 1, "maxLength": 512 },
                "revision": { "type": "integer", "minimum": 0, "maximum": 9007199254740991 }
            }
        },
        "contextRevision": { "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
        "codec": { "type": "string", "pattern": "^elu-[a-z0-9][a-z0-9.-]{0,63}$" },
        "compression": { "enum": ["none", "gzip"] },
        "contentEncoding": { "const": "base64" },
        "payload": { "type": "string", "minLength": 1 },
        "privacy": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "policyRevision",
                "effectivePolicyHash",
                "maskingProfileHash",
                "appliedBeforeSerialization",
                "secureInputsMasked",
                "platformFallbackApplied"
            ],
            "properties": {
                "policyRevision": { "type": "string", "minLength": 1, "maxLength": 128 },
                "effectivePolicyHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
                "maskingProfileHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
                "appliedBeforeSerialization": { "const": true },
                "secureInputsMasked": { "const": true },
                "platformFallbackApplied": { "type": "boolean" }
            }
        },
        "versions": { "$ref": "version.schema.json" }
    }
}
